# dev-workflow 插件使用说明

> 版本 **0.0.1** | 宿主平面静态行(入口常驻 + 功能按需挂载) | 七个模型工具 + 24 份随包技能(api-architect + 五个角色点名的 23 份联动技能) | 设置里有总开关「dev-workflow 预设」 | 已安装于
> `~/.dsh/profiles/web/node_modules/dsh-plugin-dev-workflow/`
> 「工作流模式」预设本体在 **profile 内**:`~/.dsh/profiles/web/agent-presets/dev-workflow/`(见 §零 / §八)
>
> 它解决什么:把"多个 AI 角色协作做一个软件项目"从**靠模型自觉转述**,
> 变成**由插件保证送达**,并留下可审计的台账。
> 每个角色是一个独立子会话(带自己的角色人格),角色之间的互呼由插件自动投递;
> 等待关系、熔断、仲裁、台账全部落盘,跨重启保留。
>
---

> 📁 **本文件只讲「怎么用」。** 下文出现的「见 §X」这类指引,指的都是本文档里的同名小节。

## 零、总开关(设置 → dev-workflow 预设)

设置左侧导航最后一项「**dev-workflow 预设**」是本插件的总开关(卡片样式与「皮肤」同一套)。
**开关随插件发布** —— 装了这个插件就有,不需要另装东西。

| 开关 | 预设名册 | 插件功能 |
| --- | --- | --- |
| **开**(默认) | `dev-workflow`(显示名「工作流模式」)出现在 **设置 → Agent 预设** 与新会话的选择里 | 功能已挂载:七个工具 + 24 份随包技能可用 |
| **关** | 预设目录被**改名搬到** `<profile>/agent-presets/.disabled/dev-workflow`,名册里不再显示它 | 功能从进程里**卸下**:七个工具与随包技能当场注销,**功能模块也不会被 import** |

> **两条硬要求**(由 §8.0 的安装段保证,不是靠手写组合):
> ① 只有**装了本插件**的 profile 才显示「工作流模式」预设与设置里的这个开关;
> ② 开关**关着**就不显示「工作流模式」。
> 名册的默认根 `$DSH_HOME/.agent-presets` 是所有 profile 共用的,而本插件按 profile 安装 ——
> 作用域不一致,所以安装时会额外给该 profile 挂一个**私有名册根**。

**「停用」不是「删除」。** 关掉只是把运行中的功能摘下来 + 把预设搬走,磁盘上什么都不删:

| 关闭后仍是原样 | 说明 |
| --- | --- |
| 插件包 `~/.dsh/profiles/web/node_modules/dsh-plugin-dev-workflow/` | 一个字节不动 |
| 预设文件 `<profile>/agent-presets/dev-workflow/{agent.cordis.yml,preset.yml}` | 改名搬进同根的 `.disabled/`,内容不变 |
| 运行态 `~/.dsh/dev-workflow/{state.json,profiles.json,ledger/}` | 原样保留(卸下前会先 flush 一次状态) |

重新打开即恢复(预设搬回原位、功能重新挂载)。**开关默认是开**;开关值存在 `~/.dsh/settings.yaml` 的 `dev-workflow:` 节,重启后保持。

> 注意作用域不一样:**开关值是全局的**(`settings.yaml` 只有一份),而**预设目录是 profile 私有的**
> (`<profile>/agent-presets/dev-workflow/`)。插件只搬自己所在 profile 的目录,所以在 A profile
> 关掉,动的是 A 的目录;别的 profile 若也装了插件,它启动对齐时同样会按这个值把**自己**的目录搬走。

怎么实现的(排查时有用):组合里那一行 `id: dev-workflow` 加载的是本包入口 `lib/index.js`,
它常驻(否则关掉之后没人能把开关打开);功能本体在 `lib/feature.js`,由入口按开关值挂载 / 卸下。
**关闭时功能模块根本不会被 import** —— 不是"加载了再空转"。开关卡片本身来自 `lib/client.js`
(浏览器半侧);设置页要看到它,宿主组合里那一行必须在,且重启过一次。

---

## 一、一分钟上手(完整跑一遍)

设当前工作区根目录就是项目根目录。

### 第 1 步:开工(**不需要建任何文件**)

开工是**零设置**的 —— 直接一句话:

```
relay action=kickoff goal="做一个任务清单 API"
```

它一次干完:确保激活 → 按 profile 派齐角色(协调者先) → 回初始 `流程状态.md` 全文(由你 `write` 落盘)。
想逐个派也行:`relay_spawn role=pm` 第一条成功的 `relay_spawn` 就会把项目激活。

> **激活记忆存在哪**:插件写不了工作区(实测,连带 initiator 也一样被拒),
> 所以"这个项目开工了"记在插件私有状态 `~/.dsh/dev-workflow/state.json` 里。

**`docs/workflow/.active` 仍然支持,但降级为可选项**,三种用途:

```
active                  # 显式激活(和自动激活等价,只是留个可提交进仓库的标记)
stateName=需求名         # 多流程并行:状态文件切到 流程状态-<需求名>.md
profile=lean3           # 固定这个项目的档案
off                     # 显式关闭:就算之前自动激活过,也一律回到隐身
```

> **激活门**依旧是刻意的:没激活的项目里,状态类工具一律返回 `inactive`、不生成任何文档,
> **24 份随包技能**(`api-architect` + 23 份角色联动技能)也不出现在技能目录里。
> `relay` 不受这道门限制。
>
> ⚠️ **插件不是"对普通项目完全隐身"**:激活门只挡**技能**,挡不住**工具** ——
> 7 个工具是 apply 时无条件注册的全局行(schema 合计约 8.1 KB),任何项目、任何会话都看得见,
> 只是未激活时大多返回 `inactive`。DSH 的 `tools` 服务契约里没有"按项目条件可见"这一档
> (`register` 没有可见性谓词;`restrict` 只作用于调用方 scope;`guard` 只在执行期拒绝、
> 不移除 schema),所以工具描述已按这个前提瘦身。

### 第 2 步:开工检查

```
workflow_state_status
→ workflow_state_status → active | 项目根:F:\... | 激活门:active(插件记忆(首用自动激活)) | profile:standard
```

没激活时它会**直接告诉你零设置怎么办**,而不是让你去建文件:

```
→ workflow_state_status → inactive | 激活门:inactive(未激活)
   未激活 —— 但**不需要建任何文件**:直接 relay action=kickoff goal="<一句话需求>" 一次派齐角色,
   或 relay_spawn role=<角色> 逐个派,首用即自动开工(记在插件状态里)。
```

### 第 3 步(可选):选档案

```
relay action=profile                 # 看当前档案 + 可用列表
relay action=profile profile=lean3   # 切换(写进插件状态,按项目记住)
```

### 第 4 步:派角色(逐个派的方式,kickoff 已代劳时跳过)

```
relay_spawn role=pm   prompt="理需求:……"
relay_spawn role=arch prompt="等 pm 的需求,先读 docs/workflow/项目经理.md"
→ relay_spawn spawned
  角色 @arch(② 架构师)
  provider=spawn  agentId=936cb828-…  profile=standard
  人格:✓ 已按 profile 注入 297 字(不再顶着父会话默认人格)
  继承的父会话 preset:standard(DSH 由父继承,与 wf-* 无关)
```

### 第 5 步:角色互呼(**自动投递,不用再调 `send_message`**)

```
relay action=send from=pm to=arch msg="表结构请确认"
→ relay done
  coordination=proceed
  投递:✓ scheduler → agent=936cb828-… (msg=70e21112)
  后续动作:
    - 已投递给 @arch(scheduler),你无需再调 send_message
    - 收到回覆后用 relay action=send from=arch to=pm msg=<回覆> 闭合等待关系
```

角色回话时,插件**自动闭合**原发起方的等待:

```
relay action=send from=arch to=pm msg="确认无误"
→ relay done
  回复闭合:@pm 的等待关系已释放
```

### 第 6 步:落状态文档

```
workflow_state_save role=arch nextStep="④ 质检员" outputs=["架构:docs/workflow/架构师.md"]
```

返回 `BEGIN/END` 之间的**全文**,由你用 `write` 落 `docs/workflow/流程状态.md`。

> **为什么插件不自己写**:插件发起的文件写入没有 actor,DSH 沙箱一律拒绝
> (`FS_SANDBOX_DENIED`,实测换任何路径都一样)。所以分工是:**插件负责计算与合并,会话负责落盘**。

### 第 7 步:台账投影(需要时)

```
relay action=ledger
```

同样取 `BEGIN/END` 之间全文,`write` 到 `docs/workflow/协作台账.md`。

### 第 8 步:出异常时

```
relay action=status            # 等待图 / 角色绑定 / 熔断计数 / 投递统计 / 活动统计
relay action=release from=be   # 手动释放某个卡住的等待关系
relay action=arbitrate         # 取出并清空"熔断升级"的仲裁事件
```

---

## 二、四个核心概念

| 概念 | 是什么 | 关键点 |
|---|---|---|
| **激活门** | 项目"开工了没"的判定,**四种来源**(见 §4.11):`docs/workflow/.active` 文件 ∥ 插件记忆(首用自动激活)∥ 项目里已有 `流程状态.md` ∥ `dev-workflow` 会话预设 | 都不满足 → 状态类工具 `inactive`、技能隐身。`.active` 仍是可选项,可携带 `stateName=` / `profile=`,写 `off` 则**显式关闭** |
| **profile(档案)** | 一份**纯数据**:角色集 + 每个角色的 persona/只读/模型 + relay 规则 + 状态文件路径 | 运行时可切、按项目记忆、无需重启;内置三档案,可自定义 |
| **角色子会话** | `relay_spawn` 用 `subagents.startContinuable` 派出的 durable 子会话 | 人格由 profile 的 `persona` **显式注入**;durable id 记在插件状态里,重启后仍可投递(冷恢复) |
| **等待关系 / 熔断 / 仲裁** | `A 等 B 回答` 的关系图,加滑动窗口熔断 | 同一角色同时只等一个目标;每对角色每项目 10 分钟 5 次;超限/死锁升级仲裁队列 |
| **随包技能** | `skills/` 下共 **24 份**:`api-architect`(SKILL.md + 5 份参考件)+ 五个角色点名的 23 份联动技能(每份是 `SKILL.md` + 自己的 `references/`、`templates/`、`scripts/`) | 由插件以**一个 provider** 注册进 DSH 技能目录;**跟着激活门走**,普通项目里一份都不出现;`cfg.apiSkill` 管 api-architect、`cfg.roleSkills` 管那 23 份(各可 `always`/`off`);角色技能还会按**当前档案的角色集**收窄(见 §4.9 与 §3.5) |

### 内置三档案

| id | 标签 | 角色集 | 协调者 | 熔断 |
|---|---|---|---|---|
| `standard` | 标准五角色 | pm / arch / be / fe / qa | pm | 5 次 / 10 分钟 |
| `lean3` | 精简三角色 | pm / be / qa | pm | 5 次 / 10 分钟 |
| `review` | 评审双角色(只读) | arch / qa(都是只读工具集) | arch | 8 次 / 10 分钟 |

---

## 三、工具参考

### 3.1 `relay` —— 互呼编排(10 个 action,含一句话开工)

| action | 作用 |
|---|---|
| `send`(默认) | 发起一次互呼并**自动投递**到目标角色 |
| **`kickoff`** | **一句话开工**:确保激活 → 按 profile 派齐角色(协调者先)→ 回初始 `流程状态.md` 全文 |
| `status` | 等待图、角色绑定、熔断计数、投递统计、活动统计、激活来源、私有状态健康度 |
| `profile` | 查看当前档案 + 可用列表;带 `profile=<id>` 则切换 |
| `ledger` | 渲染协作台账全文(插件写不了盘,取全文用 `write`) |
| `release` | 释放某个角色的等待关系(`from=<角色>`) |
| `deactivate` | **撤销开工记忆**(**同时解绑所有角色**、**清空等待图**,回执 `releasedWaits`):把项目从插件记忆里移除并清空 `roleAgents`/`waiting`;`.active` 文件与已有流程状态文档仍会激活它。不清绑定的话,下次 kickoff 会把死绑定当"已登记"跳过,报"人齐了"却一个都派不动;不清等待图则新协调者会看到"有人在等我"的假象 |
| `arbitrate` | 取出并清空仲裁队列 |
| `providers` | 列出可用 subagent provider |
| `presets` | 查人工入口预设(`wf-*`)装没装 —— **可选项**,角色子会话不依赖它;只有"装了但组合坏了"才报警 |

**`kickoff` 的语义**

```
relay action=kickoff goal="做一个任务清单 API"                 # 派 profile 全部角色(standard=5 个)
relay action=kickoff goal="…" profile=lean3                    # 用精简档案(3 个)
relay action=kickoff goal="…" roles=["be","qa"]                # 只派这些;协调者(@pm)不在列表里会自动补上
relay action=kickoff goal="…" force=true                       # 已登记的角色也重派
```

- **幂等**:已登记的角色跳过(`skipped`),不会重复派;响应里逐个列出 `spawned / skipped / failed`。
- **协调者必派**:它是回话入口(回给它是原生父边);被自动补上时 `coordinatorAdded=true`。
- **只透传整批通用的开关**(`force` / `provider`);`persona` / `model` / `readonly` 一律按 profile 里
  每个角色自己的设定 —— 否则一个 persona 覆盖会把五个角色变成同一个人。
- **已有 `流程状态.md` 时不覆盖**:回 `existingState`,由你先 `workflow_state_load` 读回来。
- `.active` 写了 `off` 时,kickoff 直接拒绝(`status: inactive`)。
- **项目根与调度者 cwd 不同时**:开场白里会自动补一段「【项目根】… 调用工具一律带 `root=`」,
  回执里会多一行 `📌`。因为 DSH 的子会话 **cwd 继承父会话**(spawn 契约里没有 cwd 字段),
  不给这个提示,角色会跑到调度者的目录里干活。

开工回执长这样:

```
开工:profile=standard | 协调者=@pm | 需求:做一个任务清单 API
  ✅ @pm(① 项目经理)→ 8aa1… [ ]
  ✅ @arch(② 架构师)→ 3c02…
  ✅ @qa(④ 质检员)→ 71fe… [只读]
  🔓 本项目已自动激活(不需要 .active 文件)
-----BEGIN 流程状态.md-----
…
```

**参数**

| 参数 | 说明 |
|---|---|
| `from` | 发起角色;省略时按调用者 agent 的登记身份推断,根会话默认协调者 |
| `to` | 目标角色(或由 `answer` 末行 `@relay:<角色> <问题>` 解析) |
| `msg` | 互呼正文 |
| `answer` | 角色回答原文:自动取**最后一行**合法 `@relay:` 标记作为本次互呼 |
| `targetAgentId` | 直接指定目标 durable id,跳过角色登记表 |
| `project` | **只作人读标签**(写进台账/仲裁事件)。熔断计数的分桶键**固定**是 `profile@项目根`,传它换不掉桶 |
| `root` | 项目根;不传时按 agent cwd → shell cwd → 进程 cwd 顺序解析 |
| `profile` | 本次调用使用哪个档案(不改变项目记忆) |
| `release` | `true` = 本次 send 成功后立即释放发起方的等待 |
| `noDeliver` | `true` = 只做编排判定、不投递(由模型自己 `send_message`) |

### 3.2 `relay_spawn` —— 派角色

| 参数 | 说明 |
|---|---|
| `role` | 要派出的角色(必填) |
| `prompt` | 初始指令 |
| `provider` | subagent provider;默认挑名字含 `spawn` 的 |
| `readonly` | 只读工具集;默认取档案里该角色的设定 |
| `force` | 已登记时强制重派 |
| `root` / `profile` | 同 `relay` |
| `persona` | 覆盖档案里的角色人格文本 |
| `model` | 覆盖模型 id |

只读白名单:`read` / `glob` / `grep` / `skill` / `relay` / `send_message` / `list_agents` / `workflow_state_status` / `workflow_state_load` / `api_contract`。
(`api_contract` 是只读工具,必须在白名单里 —— qa 在 standard 档案里就是只读角色,而它的人设要求跑 lint/checklist。)

> ⚠️ **`readonly` 的边界(如实告知)**:它收敛的是**这个角色自己手上的工具**,
> 不是"这个角色改不了项目"。实测:一个 `readonly=true` 的 @qa 会话**自己**没有 shell / write,
> 但它**经 subagent 通道拿到了真实 shell**(并据此跑了命令、也具备了写盘能力)——
> 也就是说,**只读角色仍可通过派子会话去改动项目**。把 `review` 档案或 `readonly=true`
> 当**隔离边界**是不成立的;它的实际语义是"**这个角色不直接持有写工具**"。
> 白名单由 `toolFilter` 施加,它管不到角色自己派生的子会话。

### 3.3 `workflow_state_*` —— 状态文档

| 工具 | 作用 |
|---|---|
| `workflow_state_status` | 开工第一步:是否激活、profile、进度、未完成待办、台账行数 |
| `workflow_state_load` | 跨会话续跑:读回进度 / 产出 / 待办 / 契约台账 / 遗留风险 |
| `workflow_state_save` | 渲染新全文(增量合并),返回 `BEGIN/END` 全文由会话落盘 |
| `workflow_state_use` | 多流程并行:切到 `流程状态-<需求名>.md` |

**状态文件落点** —— `status` / `load` / `save` / `use` 都按同一条规则解析:

1. 显式传了 `stateName=<需求名>` → `流程状态-<需求名>.md`;
2. 否则读 `docs/workflow/.active` 里的 `stateName=`(`workflow_state_use` 的落盘指示就是往那儿写);
3. 都没有 → 主 `流程状态.md`。

`workflow_state_save` 也接受 `stateName`;
显式给了 `stateName` 而门里不一致时,落盘指示会附一句"并把 `.active` 写成 `active / stateName=…`"。
要知道"切到哪一份、是不是真实内容",看返回的 `targetPath` 与 `existingState`。

`workflow_state_save` 的增量合并项:`role` / `nextStep` / `projectName` / `outputs` / `apiSpecs` / `todos` / `risks` / `contractRevision{content,affected,confirmedBy}` / `currentText`(插件读不到时由会话传入现状全文) / `stateName`。

### 3.4 `api_contract` —— API 契约(api-architect 的可执行一面)

| action | 作用 |
|---|---|
| `guide`(默认) | 选范式(REST/GraphQL/gRPC/WS)、响应信封、版本化三选一、错误码表 |
| `template` | 取模板全文:`kind=openapi\|graphql\|proto\|ratelimit\|security`;回 `documentText` + 落盘指令 |
| `lint` | 递归扫描项目里的契约文件做校验(ERROR>0 → FAIL;**一个都没扫到 → `no_specs`,不算通过**;**有候选被跳过 → `pass_with_skips`,也不算通过**),结果连同**契约指纹**与**覆盖面**记进插件状态 |
| `checklist` | 契约侧 + 落地侧两份核对表(④ 质检按它逐条核) |
| `status` | 项目里有哪些 spec、上次 lint 结论、**指纹是否漂移**、技能当前是否可见 |

**lint 的四条口径**

- **空集不算通过**:一个契约都没扫到 → `no_specs`。
- **覆盖面不足不算通过**:有候选被跳过(超 512 KB / 超 2 层深度 /
  读失败)时,那条"ERROR 0"只对**扫到的**候选成立 → verdict 降级为 **`pass_with_skips`**,
  回执上**结论行 / 扫描行 / 状态面板 / 指纹旁**一律带**覆盖面**。
  **计数口径**:跳过按"**处**"算 —— 一处 = 一个文件(oversize / unreadable / other)
  或**一棵未展开的子树**(depth;同一片子树被多次 walk 记到时**只算一处**,保留最深的那条 rel,
  因为基址更深的 walk 已经把它上层的文件扫过了)。**只有分母精确时才给分数**:
  跳过全是文件 → 照旧 `M/N(P%)`;只要有一棵子树,它里面有几个候选是**未知数** → 不给分数,
  改说"已扫到的 M 个候选(另有 S 处未扫到,子树内容未知)"。把一棵子树当 1 个候选塞进分母则会打出
  **高估**:一棵装着 50 份契约的子树会被算成 1,于是"覆盖 8/9(89%)"照样打得出来
  (`slot.apiLint.coverage` 同时记 `files` / `regions` / `exactTotal`)。
  两条出口:① `depth` 类可以用 `paths` **精确指到文件**补进结论;② `oversize` 类**精确指路无效**
  (size 守卫对 `paths` 同样生效),只能拆到 512 KB 以下,或确认它不属于本项目契约后
  在「API 契约」小节**显式登记为未校验 + 原因**(登记是允许的,静默算过不行)。
  只有"本来是通过"的两种判定会降级;`fail` 与 `no_specs` 保持原判(覆盖面照样渲染)。
- **计数不封顶,明细可枚举**:留痕要能一条条枚举,所以跳过记录的**明细**
  只留前 20 条;但**计数不封顶**(`found.skippedTotal`,同时挂在明细数组自己身上)。两者对不上时:
  所有"处 / 个"的数字**加 `≥` 写成下界**(被截掉的那几条可能是文件、也可能整棵子树 —— 连折叠都
  算不了它们),`exactTotal=false`(**即使一棵子树都没有也不给分数**:分母里少了没有明细的那些),
  并且留痕会紧跟总述行交代「**明细已截断**:本次共记下 M 条跳过记录(同一片区域被不同基址的扫描
  各记一次时会各算一条,所以**记录条数 ≥ 处数**),留痕只保留前 20 条明细;另有 K 条**连明细都没有**」。
  ⚠️ `payload.scanSkipped` 是**明细**(≤20 条),**不是全量** —— "数据面字段 `scanSkipped` 里有全量"
  在截断时是一句**做不到的指引**(照着它去翻 payload,只会看到同样那批,于是"合理地"得出错数);
  只有"源头没截断、只是每组显示上限"时,这句话才成立。
- **`action=status` 的 note 行也走同一口径**:它与面板、`上次 lint` 行**三处同源**,一律经
  `apiCoverageText` —— 不会出现"同一屏里 note 行照旧给分数(`覆盖 1/20`),另两行却在说
  `≥19 处未扫到`"这种自相矛盾。没有存 `exactTotal` 的旧记录照旧渲染 `1/4`。
- **归属校验**:把包内参考件原文照抄到 `docs/api/` 不再无条件 PASS —— 与参考件逐行重合 ≥85%
  且重合行 ≥25 时判 `vendor-template` ERROR。判据是**与包内参考件的逐行重合度**(模板改了这里跟着改),
  不是几个硬编码关键词;正常按项目改写的 spec 不会命中。
- **契约指纹**:每条 lint 记录同时存**每个 spec 的 SHA256** 与整批指纹;
  `action=status` 会用当前磁盘内容重算指纹并与记录对账,不一致就报 `specDrift: true`
  并在文案里说明"上次那句结论已经作废,请重跑 lint"。**"lint ERROR 0"从此是对某一批具体文件成立的**,
  spec 被整体换掉后旧结论会自动失效。

**典型链路(② 架构师)**

```
skill name=api-architect                      # 加载规范(激活门开着才在技能目录里)
api_contract action=guide                     # 先定范式/信封/版本化
api_contract action=template kind=openapi      # 取 OpenAPI 3.1 模板全文
→ 主会话用 write 落到 docs/api/openapi.yaml     # ⚠️ 模板原文会被 lint 判 ERROR,必须按项目改写
api_contract action=lint                      # 必须 ERROR 0(记录里带指纹)
workflow_state_save role=arch apiSpecs=["docs/api/openapi.yaml | 类型=openapi | 版本=/v1 | lint=ERROR 0 | 指纹=<前12位>"]
```

**参数**:`action` / `kind` / `paths`(**追加**到默认候选目录之外,不是"只扫这里";超过 512 KB 的文件即便精确指到文件路径也会被跳过)/ `root` / `profile`。

**lint 扫哪里**(名单以代码常量为准:目录 = `API_SCAN_DIRS`,扩展名 = `API_SCAN_EXT`,改代码时这里要跟着改):
项目根下 `api/`、`openapi/`、`docs/`、`docs/api/`、`docs/apis/`、`proto/`、`graphql/`、`schema/`、`spec/`、`specs/`、`contracts/` 与根目录
(深度 ≤ 2,跳过 `node_modules` / `.git` / `dist` / `build` 等,最多 80 个文件、**单文件 ≤ 512 KB** —— 超限的会被**跳过**并在回执里留痕),
认 `*.yaml | *.yml | *.json(仅契约)| *.graphql | *.gql | *.graphqls | *.proto`。判定口径:
**ERROR>0 → FAIL;WARN>5 → 通过但有告警**。另有两道**防空洞**:"**一个契约文件都没扫到时 verdict = `no_specs`**"
与"**有候选被跳过时 verdict = `pass_with_skips`**"(见上"覆盖面不足不算通过"),两者都**不算通过** ——
否则"压根没写 spec"或"契约明明在却扫不到"的项目会拿到 `✅ PASS | ERROR 0`,把"lint 必须 ERROR 0"这道门架空。
插件写不了工作区,所以模板与 spec 一律由**会话**用 `write` 落盘。

### 3.5 随包技能

角色人设里那行「联动技能(按需加载)」指的就是这批 —— 它们随插件发布在 `skills/` 下,由**同一个
技能提供者**注册进 DSH 技能目录。用 `skill name=<技能名>` 加载,加载后正文里的
`references/`、`templates/`、`scripts/` 都相对 **"Base directory for this skill"**(= 该技能自己的目录)。

| 角色 | 联动技能(按需加载) |
|---|---|
| ① `pm` | `grill-me`(需求澄清:2-3 问,问完即走) |
| ② `arch` | `api-architect`、`architecture-diagram`、`excalidraw`、`ponytail`、`grill-me` |
| ③ `be` | `ponytail`、`systematic-debugging`、`test-driven-development`、`spike`;**按项目技术栈挑一路** —— Java/Spring:`spring-boot-project-creator` / `spring-boot-crud-patterns` / `spring-boot-rest-api-standards` / `spring-boot-test-patterns`;Python/Django:`django-patterns` / `django-security` / `django-tdd` / `python-project-structure` / `python-error-handling` |
| ③' `fe` | `frontend-design`、`impeccable`、`ui-ux-pro-max`(挑 1-2 个,不一次全挂) |
| ④ `qa` | `requesting-code-review`、`testing-strategy`、`api-docs`、`spring-boot-test-patterns` / `python-testing-patterns`(按技术栈挑);前端验收时 `frontend-design` / `impeccable` / `ui-ux-pro-max` 挑 1-2,后端项目不加载 |

四条**会影响到你看到什么**的规矩:

1. **跟着激活门走**:未激活的项目里 24 份一份都不出现(与 `api-architect` 同门);
   `cfg.roleSkills: "always"` 可让它对**所有**项目现身,`"off"` 则整批不注册。
2. **按当前档案的角色集收窄**:`lean3`(pm/be/qa)里没有 @arch 这个角色,`excalidraw` /
   `architecture-diagram` 就不进目录;`review`(arch/qa)里 `test-driven-development` / `spike`
   这些 be 专属技能同理。切档案后**当场生效**(下一次目录刷新就变,不用重启)。
   前端三件套的归属含 `qa`(④ 前端验收要用),所以 `lean3` 里它们仍在。
3. **改名/改正文立刻生效**:技能正文是**读文件**得到的(不是编译进代码的字符串),
   改 `SKILL.md` 不用重启宿主;frontmatter 的 `description`(技能目录里显示的那行)同理。
4. **"谁能看见"按 `cwd` 现算,而且状态面与提供者**同一条链**:提供者拿到的只有
   调用方 `cwd` —— 这个 `cwd` 自己激活就用它,否则反查"这个 `cwd` 上有没有**活跃项目的角色子会话**"
   (角色子会话继承调度者的 `cwd`,所以调度者会话通常也看得到那批技能)。两种情形在
   `workflow_state_status` / `relay action=status` 的「角色技能」行里**分别写明来路**;
   反查不到就如实报「隐藏(本 root 未激活…)」。**行里报的份数 == 目录里真有的份数**。

> **rank 口径(谁盖过谁)**:`api-architect` 用 rank 60 —— 它必须与 `api_contract` 工具**同源**
> (模板、lint 规则、核对表都长在同一份文本上),被别的同名技能盖掉会让工具与技能说两套话。
> 那 23 份用 rank 550:盖过部署自带的那一份(600),但**让位给用户/项目自己放的同名技能**
> (project-dsh 100 / project-agents 200 / custom 300 / user-dsh 400 / user-agents 500)。
>
> **版本口径**:24 份 SKILL.md 的 frontmatter `version` 与插件版本对齐,
> 部署后一眼能看出"手上这份技能是哪一版插件带来的"。

---

## 四、必须知道的约束(踩坑点)

### 4.1 邻居约束:兄弟角色不能直连,由插件代偿

DSH 在**服务层**强制 `sendMessage` 只能沿父子边,兄弟角色物理上无法直连。插件的做法是不消除它,而是**以调度者身份中转**,对模型就是直呼。三条投递路径:

| via | 场景 |
|---|---|
| `scheduler` | 根会话(调度者)投递给自己的角色子会话 |
| `scheduler-proxy` | 角色发给**兄弟**角色时,插件以父会话(调度者)身份投递 |
| `to-parent` | 角色回话给协调者(默认 pm)= 它的父会话,原生边 |

### 4.2 角色绑定绑定在**父会话**上 —— 换会话就要重派

角色是"某个父会话的子"。换会话(或重启后新开会话)再投递会得到:

```
UNAUTHORIZED(belongs to another parent session)
```

→ 重新 `relay_spawn` 即可。插件在失败提示里已写明这一点。

### 4.3 等待关系有三条释放路径(不会永久卡死)

| 路径 | 规则 |
|---|---|
| 回复闭合 | B 回覆正在等它的 A(且 B 自己没在等别人)→ A 的等待自动释放,**不判死锁** |
| 超时释放 | 等待超过 15 分钟(可按档案配 `waitTimeoutMs`)→ 自动释放并记台账 `⏱ 超时释放` |
| 显式释放 | `relay action=release from=<角色>` |

真死锁(双方互等)仍会被抓并熔断升级仲裁。想关掉这条:档案里加 `"replyClosesWait": false`。

### 4.4 去重与熔断

- **去重**:同 `profile + 项目 + from + to + 内容` 在 60 秒内只投一次,重复请求返回 `relay duplicate`(可用 `noDeliver` 绕过)。
- **熔断**:每对 `from→to` 在每个项目 10 分钟内 5 次(档案可配)。超限**不投递**,事件进仲裁队列。
- **忙等**:**不丢弃正文**。同一角色已在等某人时再发一次互呼,正文照样投给目标,
  等待图不被改写,返回 `busy`。回执里会告诉你"已并线送达"。

### 4.5 熔断/忙等通知"不自投"

被拦下的互呼会把通知投给发起方;但若**发起方就是调用者自己**,通知不投递(工具结果里已经有),避免自我插话。

### 4.6 统计有两套口径,各自带标签

`relay action=status` 报两组:

- **`活动统计:进程累计(全部项目):…`** —— `工具调用(已完成) / 在飞` 统计的是**整个 DSH 进程里所有会话**的工具调用(含子会话);
  这里的 `最后错误` 也是全局的(任何项目里某次工具调用失败都会写进去),文案里已明确标注,别把它读成"本项目出错"。
- **`本项目统计:本项目(profile@根):…`** —— 状态保存 / 互呼 / 派出 / 投递成功失败 / 去重 / 并线 / 失效重派 / 最后错误,
  全部**按项目(档案@根)分桶** —— 一个项目的失败数与错误不会串到别的项目上。

### 4.7 人格来自 `persona`,预设是可选项(默认不装)

DSH 的子会话人格**继承父会话的 preset**,与 `relay_spawn` 传什么都不相干。所以角色子会话的人格只能由 `request.persona` 显式注入 —— 来源是档案的 `persona` 字段。

推论:`wf-*` 角色预设**对流程没有任何作用**,它只服务一个场景 —— 人工自己从 GUI 选一个预设、以某角色身份开会话。默认不安装它们,于是:

- 人格只在 `PERSONAS` 一处维护(预设里那份副本漂移会被同源测试抓到);
- `relay action=presets` 在"一个都没装"时不再刷五行 ❌,只回一行说明;只有**装了但组合坏了**才报 `incomplete`;
- 想恢复人工入口:照 §五「自定义档案」自建对应角色的预设,放进 `~/.dsh/.agent-presets/`。

### 4.8 台账是审计件,落盘靠会话

插件保留最近 200 行;`relay action=ledger` 渲染成 Markdown 表格(新→旧)。插件写不了工作区,取全文 `write` 落盘。
`noDeliver` 的记录会如实标 `✅ 已判定(投递由模型执行)`,不会谎报"已转发"。

### 4.9 技能注册也走激活门(一个提供者交两批技能)

24 份随包技能都是用 `ctx.skills.registerProvider` 注册的 **runtime 技能提供者**,不是静态注册:
`list({cwd})` 里现算激活门 —— 没开工就返回空,技能目录里一份都看不到。
这样"插件对普通项目完全隐身"的性质**在技能侧**成立(注意:只有技能侧 —— 7 个工具不隐身)。
激活门状态翻转时(建/删 `.active`、或首次自动激活)插件会主动 `invalidate()` 一次,让目录缓存跟着变;
技能内容本身是**读文件**得到的(不是编译进代码的字符串),所以改 `SKILL.md` 立刻生效,不用重启。

这两批技能由**同一个提供者**交出(提供者名在同一层里必须唯一,24 个提供者就要 24 个名字;
而 `get(candidate)` 本来就收 candidate,按 `locator.name` 分流更省,也保住了"激活门一翻,
两批技能在同一次 `list` 里一起出现/一起消失"这条不变量)。两条补充口径:

- **按档案角色集收窄**(只作用于那 23 份):技能提供者的 `list()` 只拿得到调用方 `cwd`、
  拿不到 agent 身份(与 tools 注册同一条约束),所以口径取"**这个项目用哪份档案、这份档案有哪些角色**" ——
  与 `relay_spawn` 能派谁**完全一致**。lean3 里 `excalidraw` 这类 arch 专属技能不进目录;
  前端三件套的归属含 `qa`,所以 lean3 里仍在。要全量可见:`cfg.roleSkills: "always"`。
- **可见性 / 收窄档案 / 份数走同一条链**:`list()` 只拿得到 `cwd`,所以"可见不可见"的判据
  就是"这个 `cwd` 能不能反查到一条**活跃项目的角色绑定**"(`skillRootForCwd`);状态面
  (`roleSkillsSummary`)走的是**同一条链**,所以两处不会分叉(未激活的 root 不会报出
  「23/23 个可见」,角色子会话停掉后回执也不会报出目录里没有的份数)。反查命中**别的**项目时
  把来路写进那一行
  (`…个可见(档案 lean3 —— 本 root 未激活;此 cwd 与活跃项目 X 的角色子会话同址,会话级目录按它的档案走):…`)。
- **`enableApi: false` 只关 api 那一半**:两批技能各自判自己的开关,`enableApi:false` 不会连角色技能一起关掉
  (要避免的是"技能还在、它要求调用的工具没了"这类错配)。

### 4.11 开工零设置:激活的四种来源与一条逃生阀

| 来源 | 何时生效 | 备注 |
|---|---|---|
| `.active` 文件 | 文件存在且内容不是 `off` | 显式、可提交进仓库、可携带 `stateName=` / `profile=` |
| **插件记忆** | 首次 `relay_spawn` 成功、`relay action=kickoff`、或 `workflow_state_save` | 落在 `~/.dsh/dev-workflow/state.json` 的 `activeProjects`;**不需要用户做任何事** |
| 项目里已有 `流程状态.md` | 前三者都不满足时兜底 | 跨机器/跨克隆自愈:只要项目里有流程状态文档就认 |
| **会话预设** | 会话**运行时**的 agent preset(读 Session 投影 `agentPreset`,header 兜底)命中 `autoActivatePresets`(缺省 `['dev-workflow']`) | 入口四条:`session/event` 与 `agent-preset/selected`(预设提交那一刻,零工具调用)+ `agent/created` + `tools/execute` 兜底;写进的是上一条**同一个 `remembered` 档**,所以技能可见性 / 档案解析那批按门判定的路径都不用改 |

> **为什么读 Session 投影而不是 header**:header 是**会话创建那一刻的化石** ——
> DSH 的 Web 入口先按默认预设建会话(header 写 `cordis`),用户选的预设是**随后**作为
> `agent-preset/selected` 事件追加的。只看 header 会让这条来源**静默失效**。
> 口径照抄 DSH 自己:*Reconstruction reads the `agentPreset` Session projection, never the header*。

**第四种来源的三条边界**,每条都有断言守着,不是"应该不会":

1. **只在自己的会话里生效** —— 不命中的预设,激活方式与其余来源完全一样。
2. **只判一次,不是持续强制** —— 进入预设时记一笔;同一会话里 `relay action=deactivate` 之后
   **不会被顶回去**。要长期关掉用下面的逃生阀。
3. **不抢功** —— 靠 `.active` 文件激活的项目不会被写进插件记忆,来源仍显示"文件",
   免得排障时分不清这项目当初是谁开的。

**这一档是可观测的**:上面三条边界若不留痕,判定被门挡回去时连一行日志都没有,
"正确地没激活"与"判定压根没跑到"在外面就长得一模一样。所以:

- **一段进程内计数**(不落盘;落盘会把"本进程入口健康度"变成跨重启的累计值,那就答不了它要答的问题):
  `到达`(四条入口各几次)/ `判定`(过了去重、真的判了几次,按入口分)/ `结局`(激活 / 门已定 /
  不命中清单)/ `没判成`(根未就绪 / 查不到会话 / 清单已关)/ `最近一笔`(可读描述)。
  ⚠️ `tools/execute` 每次工具调用都会"到达",所以它数字大是正常的 —— 要看的是**判定**那一列。
- **两行可分辨的宿主日志**(判定一次打一行,并写明"本会话在该预设下不再重判"):
  · 门已被别的来源定下 → `预设命中但门已定:<预设> 预设的会话在 <root>(gate=remembered|file|state-doc|off),不抢功(via=…)`
    —— 措辞是**门已定**不是"门已开":`off` 那一档是用户**显式关闭**(门是关着的),四档的共同点只是
    "门已经被别的来源定了";
  · 预设不在清单里 → `预设不命中清单:<预设> 预设的会话不激活(清单=…;via=…)`
    —— 判定发生在**取根之前**,所以"不命中的预设不可能记下任何目录"是**结构性**成立的。
- **状态面各一行**:`workflow_state_status`(含 `inactive` 分支 —— "什么都没发生"正是要看计数器的场景)
  与 `relay action=status` / `action=deactivate` 的回执里都能读到同一行文本(同源)。

**怎么用它排障**:预设下"没动静"时先看那一行 —— `判定 0 次` 说明**入口根本没到**(事件路径问题);
`判定 1 次(结局:门已定 …)` 说明**到了、但门已经被别的来源定了**(`gate=` 直接告诉你是谁);
`结局:不命中清单` 说明这个会话的预设不在 `autoActivatePresets` 里(预设字段本身要对着
`relay action=status` 的 `presetView` 看)。

优先级:**`off` > 文件 > 记忆 > 状态文档 > 未激活** —— 所以 `.active` 写 `off` 依旧压过第四种来源。
配置项 `autoActivatePresets` 收字符串或数组;传 `false` / 空串 / 空数组即整个关掉。

**为什么只能记在插件状态里**:实测插件发起的 `fs.writeText` 一律被拒 ——
`FS_SANDBOX_DENIED ... file access denied under workspace-write`,**即使把调用者 agent 用
`agents.withInitiator()` 带进去也一样**。所以项目侧的任何文件(`.active`、`流程状态.md`、spec)
都只能由**会话的 `write`** 落盘;插件只能写自己地盘。

**逃生阀**:`docs/workflow/.active` 里写一行 `off`(或 `inactive` / `false` / `0`),
则无论有没有记忆、在不在自动激活清单里的预设下一律回到隐身 —— 适合"这个项目我不想让它自动开工"。
`gateStateOf()` 会把当前来源报给 `relay action=status`:`激活:remembered(插件记忆(首用自动激活))`;
若是预设开的,则显示 `激活:remembered(预设 dev-workflow(自动激活))` —— 两种来源**分开显示**,
免得混成同一个词、排障时分不清是谁开的。

**撤销自动激活**:`relay action=deactivate` 把项目从**插件记忆**里移除(回到隐身)。
注意它只管记忆:`docs/workflow/.active` 文件与项目里已有的 `流程状态.md` 仍会激活该项目 ——
要彻底关掉就读 `off` 或删掉文件。在自动激活清单里的预设下,`deactivate` 在**本会话内**
站得住,但**下一个**该预设下的会话会重新记一笔 —— 要长期关掉请写 `off`(它是唯一压得住预设的来源)。

**`off` 与 `deactivate` 到底各管什么** ——
一个是**门**(最高优先的那一档来源),一个是**动作**(只动插件自己的记忆):

| | `.active` 写 `off` | `relay action=deactivate` |
|---|---|---|
| 本质 | 一个**来源**:`gateDecision` 里最高优先的一档 | 一个**动作**:只改插件记忆 |
| 改了什么 | 项目里的一个文件 | `~/.dsh/dev-workflow/state.json` 的 `activeProjects[root]` |
| 撤销范围 | **一切** —— 文件门 / 插件记忆 / **预设自动激活** / 流程状态文档全被压住 | **只撤销"插件记忆"这一档**;文件与状态文档的来源照旧开着 |
| 持久性 | 持久(文件在就一直在,还能提交进仓库) | 一次性(下一次 kickoff / 首用 / 预设会话会**重新记一笔**) |
| 谁看得见 | 任何人(项目里的一行文本) | 只有插件自己 |
| 回执 | `门判定:显式关闭(.active 写了 off)(gate=off)` | `已从插件记忆里移除开工记录…;但项目还有别的激活来源(文件 docs/workflow/.active),要彻底关掉就在 docs/workflow/.active 里写 off` |
| 顺带做的事 | 无 | **解绑全部角色 + 清空等待图**(防"记忆说没开工、绑定表说过五个人都在"的鬼状态) |

判定顺序是**先看文件、文件在就轮不到别的**:

```
文件存在?  →  内容是 off/inactive/false/0/disabled ?  →  off     ← 一票否决
              其余内容(active / stateName=… / profile=…)      →  file
文件不在?  →  插件记忆里有这个 root?                   →  remembered
              →  项目里有 流程状态.md?                 →  state-doc
              →  都没有                                →  none
```

**怎么选**:想"这个项目别自动开工" → 写 `off`(唯一压得住预设的);想"插件忘掉开过工,
顺手把角色绑定清掉" → `deactivate`;在自动激活清单里的预设下想**长期**关掉 → 只能 `off`。

### 4.12 子会话的工作目录继承父会话,项目根靠插件自己认

DSH 的 `SubagentStartRequest` **没有 cwd 字段**(实测:五个角色子会话的 cwd 全是调度者的目录),
所以 `relay action=kickoff root=<别的目录>` 派出去的角色,默认会去操作**调度者的**目录。
后果:五个角色会把 `<工作区>` 当成项目根,在里面写 `项目经理.md`/`开发者-后端.md`/`流程状态.md`,
还顺带把工作区**自动激活**。

两道防线:

1. **自动反查(结构性)**:`resolveRoot()` 在拿不到显式 `root` 时,会先看调用者是不是某个项目里
   **已登记的角色子会话** —— 是就返回**那个项目**的根(`rootOfRoleAgent()`),而不是它的 cwd。
   于是角色不带 `root` 调 `relay` / `workflow_state_*` / `api_contract` 也会落到正确项目。
2. **开场白兜底**:项目根与调度者 cwd 不一致时,`relay_spawn` / `kickoff` 生成的提示词里
   会附一段`【项目根】…`,并明确要求"调用工具一律显式带 `root=`、产出也写在这个目录下"。

非角色会话(普通子会话、根会话)行为不变,仍按 cwd 解析 —— 没有误伤。

### 4.10 契约 lint 只读不写

`api_contract` 全程**只读**:模板取全文交会话落盘,lint 只读文件。插件从不替会话写 spec ——
这也让"谁改的契约"在台账/状态文档里始终有 actor 可查。

### 4.13 环境约束:角色会自动收到,不必自己再验一遍

这台机器上的四条实测约束(**追加到每个角色的人格末尾**,`relay_spawn` 回执里 `envNotesInjected: true`):

| 约束 | 实测事实 | 后果 |
|---|---|---|
| **没有 PowerShell 7** | 只有 Windows PowerShell 5.1 | 文档里写 `pwsh -File …` 照抄必失败 → 用 `powershell -File …` |
| **PS 5.1 的 `2>` 不可信** | 抓到的是 ErrorRecord 包装文本(UTF-16LE BOM),不是程序 stderr | 要字节级证据必须 `cmd /c "… > out.txt 2> err.txt"` |
| **控制台默认 GBK** | Python 打印 `↔` 之类直接 `UnicodeEncodeError: 'gbk' codec …` | 先 `$env:PYTHONIOENCODING="utf-8"`;实测加不加它,同一条命令从 exit 1 变 `PASS 68 / FAIL 0` |
| **沙箱禁止管道 stdio** | `spawnSync` 默认 `stdio:'pipe'` → `EPERM` | `node --test` **必然失败** —— 属**环境阻断**,登记为"环境失败、通过率无法计算",不要写成代码失败 |

还有一条不是环境而是**记账**规矩:**报通过率必须带三元组(命令 + 采集时刻 + 工作树哈希)**。
实测十分钟内同一批命令给出过完全相反的结论(`PASS 59/0` → `FAIL 97/14/3`)——
缺了树哈希,两个人"都有证据"却谁也说服不了谁。

> 这些约束同时写进了角色人格(随 `persona` 注入)与本文档:角色不会去读使用说明,所以两处都要有。

### 4.14 契约台账的「确认人」会被反查

`workflow_state_save` 的 `contractRevision.confirmedBy` **不再是一个自由字符串**。写入前插件会用
**已有的编排事实**反查每个被点名的角色是否真的参与过:

| 判据(任一成立即"已验证") | 出处 |
|---|---|
| 该角色在角色绑定表里(派过会话) | `roleAgents` |
| 台账里有过 `from`/`to` = 该角色的互呼或投递记录 | `ledger` |
| 该角色在等待图里 | `waiting` |

三条都不成立 → **照写台账,但同一行标注** `⚠️ 未验证:@qa(…)`,回执里给 `unverified` 与两条可照做的补救
(`relay action=send … to=qa` → 复核回来再重新登记)。

**想表达"这是计划中的确认人",就明说**:`confirmedBy` 里带 `计划` / `待确认` / `待复核` / `未确认` / `待定`
之一即不反查。反查要抓的是"把计划写成已完成"(例如把从未发起的 @qa 复核写成已确认),
不是禁止写计划 —— 所以它**只标注、不拒绝**。

---

## 五、自定义档案

写 `~/.dsh/dev-workflow/profiles.json`(**每次调用都会重读,不需要重启**):

```json
{
  "mini": {
    "label": "迷你双角色",
    "coordinator": "pm",
    "roles": [
      { "id": "pm", "label": "① 项目经理" },
      { "id": "qa", "readonly": true, "persona": "你是本项目的独立质检:只出问题清单与补丁,不改代码。" }
    ],
    "relay": { "limit": 3, "windowMs": 600000, "waitTimeoutMs": 300000, "autoDeliver": true },
    "state": { "file": "docs/workflow/流程状态.md", "ledger": "docs/workflow/协作台账.md" }
  }
}
```

字段全部可选,缺的从内置 `standard` 继承。`roles` 里只写 `{ "id": "pm" }` 就会自动带上内置人格。

**角色字段**

| 字段 | 作用 |
|---|---|
| `persona` | 真正生效的角色人格(显式注入子会话)—— **唯一事实来源** |
| `preset` | 只是给人工入口预设留的**标签**(`wf-pm` 之类);子会话不会拿到预设,预设没装也不影响 |
| `readonly` | `true` → 子会话工具集收敛为只读白名单 |
| `model` / `provider` | 透传 `agentOptions.model` / provider 名(留空继承父会话路由) |

**组合级配置**(写在 `~/.dsh/profiles/web/cordis.patch.yml` 的 plugin 行 `config` 下):
`enableRelay: false` 可停掉全部工具;`enableApi: false` 只停掉 `api_contract`(与 api-architect 技能,**不动角色技能**);
`apiSkill: "active" | "always" | "off"` 控制 api-architect 技能的现身条件(默认 `active` = 仅激活项目);
`roleSkills: "active" | "always" | "off"` 控制那 23 份角色联动技能的现身条件(默认 `active` = 仅激活项目,
且再按当前档案的角色集收窄;`"always"` 对所有项目放开,`"off"` 整批不注册);
`stateStore: "memory"` 改为纯内存;
`ledgerAutoWrite: false` 关掉台账自动写尝试;`defaultProfile` 设默认档案;`profiles` 内联档案。

---

## 六、排查表

| 现象 | 原因 | 处理 |
|---|---|---|
| `status: inactive` | 项目还没开工(没有 `.active`、没记忆、也没有流程状态文档) | **什么都不用建**:`relay action=kickoff goal="…"` 或 `relay_spawn role=pm` 就会自动激活 |
| `status: inactive` 且 `gate=off` | `docs/workflow/.active` 写了 `off` | 删掉那一行(或整个文件)即恢复自动激活 |
| 项目被**误激活**(不该开工却被记了) | 曾经在它里面调过 `relay_spawn` / `kickoff` / `workflow_state_save` | `relay action=deactivate`;若它本来就有 `流程状态.md`,那也算激活来源,得删文件 |
| 角色子会话跑到**别的目录**去干活 | 项目根与调度者 cwd 不同,而角色没带 `root=` | 已自动反查 + 开场白提示;老会话重派一次即可 |
| `busy` | 该角色已在等别人 | 等回覆,或 `relay action=release from=<角色>` |
| `no_reply` | 目标角色没有已登记的 agent 会话 | `relay_spawn role=<角色>` |
| `NOT_RESUMABLE` / "is unavailable" | 目标子会话无法冷恢复 | 重派该角色 |
| `UNAUTHORIZED(belongs to another parent session)` | 换过会话,绑定失效 | 重派该角色 |
| `role_not_in_profile` | 当前档案没有该角色(如 lean3 里没有 arch) | `relay action=profile` 切换,或换角色 |
| `relay duplicate` | 60 秒内同一互呼 | 改措辞,或等 60 秒,或 `noDeliver` |
| `limit`(熔断) | 该对角色 10 分钟内超 5 次 | 先收敛需求;`relay action=arbitrate` 取事件 |
| `deadlock` | 真互等成环 | 等待图已拆;`relay action=arbitrate` 裁决 |
| `插件私有状态:不可写` | `~/.dsh/dev-workflow/` 不可写 | 已自动降级内存态(功能在,重启清空),把原文反馈 |
| 台账写不进项目 | 插件无工作区写权限(设计如此) | 取全文,用 `write` 落盘 |
| 技能目录里没有 `api-architect`(或没有 `ponytail` 这类角色技能) | 项目没激活(`docs/workflow/.active` 不存在) | 建该文件;急着用可临时 `apiSkill: "always"` / `roleSkills: "always"`(改组合配置需重启) |
| 角色技能少了几份(如 `lean3` 里看不到 `excalidraw`) | 那几份归属的角色不在**当前档案**的角色集里(lean3 没有 @arch) | 属正常收窄;要全量可见设 `roleSkills: "always"`,或换回 `standard` 档案 |
| 角色说"要加载某个联动技能"但目录里没有 | 该技能不随包(被裁掉)或项目未激活 | `workflow_state_status` / `relay action=status` 的「角色技能」一行会报可见数;按上两行处理 |
| 「角色技能」行报 `隐藏(本 root 未激活…)` | 本 root 没激活,**且**这个 `cwd` 上没有活跃项目的角色子会话(没派过角色,或角色子会话已经结束) | 建 `.active`,或先 `relay_spawn` 派一个角色 —— 两者都会让目录**当场**刷新(不用重启) |
| 行里写着 `本 root 未激活;此 cwd 与活跃项目 X 的角色子会话同址` | 你在**调度者 `cwd`**(常常是项目根的上一级)上调的 status,而角色子会话继承的就是这个 `cwd` | 正常现象(目录本来就按 X 的档案收窄);要看本项目自己的口径,传 `root=<项目根>` |
| `api_contract template` 报 `missing_asset` | 安装时漏了 `skills/` 目录 | 重装整个包目录(不止 `lib/`) |
| `api_contract lint` 扫到 0 个契约 | spec 不在候选目录里 | 放到 `docs/api/`,或用 `paths=[...]` 指路 |
| lint 报的 WARN 太多 | 项目 spec 本来就糙 | 看 `fix` 逐条改;ERROR 必须清零,WARN 可留但要在状态文档里说明 |

---

## 七、三个配方

**A. 完整五角色流程**

```
relay action=profile profile=standard
→ relay action=kickoff goal="<一句话需求>"        # 一次派齐五个角色 + 回初始流程状态全文(write 落盘)
→ pm 理需求(之后每步 workflow_state_save → write 流程状态.md)
→ pm 互呼 arch 确认可行性(relay action=send,自动投递)
→ arch 出 架构师.md,契约有变必须登记 contractRevision
   └ 有接口:skill name=api-architect → api_contract action=guide → action=template
     → write docs/api/openapi.yaml → api_contract action=lint(ERROR 必须 0)
     → workflow_state_save role=arch apiSpecs=[...]
→ be/fe 按契约实现,自测证据写入各自文档
→ qa 只读复检(档案 review 或 readonly=true),不合格回 be
→ relay action=ledger → write 协作台账.md
```

**B. 小需求:三角色**

```
relay action=profile profile=lean3     # pm/be/qa,砍掉架构与前端两条并行线
```

**C. 只做评审,不动代码**

```
relay action=profile profile=review    # arch + qa,两个都是只读工具集
```

---

## 八、安装(两种方式)与当前安装状态

安装 = **① 把包放到位** + **② 让组合加载它**。本插件是**零第三方运行时依赖**的 host 静态行
(`lib/feature.js` 只 import `node:fs / node:path / node:os / node:crypto / node:url`),所以第一步不牵扯依赖树。

### 8.0 还要第三件事:把「工作流模式」的名册根接到本 profile

前三节讲的两种装法都只解决 ① ②。本插件还多一件:**「工作流模式」预设的名册根必须挂在该 profile 上**,
否则 §零 那两条硬要求都保不住:

| 漏掉什么 | 后果 |
| --- | --- |
| 只声明插件行、不挂名册根 | **没装插件的 profile 也会列出「工作流模式」**(名册默认根是所有 profile 共用的) |
| 只铺预设、不挂名册根 | **装了插件的 profile 也看不到它** |

`install.mjs` 一次做完三件事:铺包 → 铺预设本体 → 在 profile 的 `cordis.patch.yml` 里写一段
**受管段**(声明 profile 私有名册根 + 给插件行加 `presetRoot`)。

```powershell
node install.mjs --dry-run              # 先看要写什么,不动文件
node install.mjs                        # 铺进 $DSH_HOME/profiles/web
node install.mjs --profile <dir>        # 铺进指定 profile
node install.mjs --take-over            # 接管旧的、手写的 dev-workflow insert 块
node install.mjs --uninstall            # 撤掉受管段(包文件与预设本体都不动)
node install.mjs --default-preset <id>  # 覆盖继承来的 default(基座找不到时的逃生阀)
node install.mjs --preset-source <dir>  # 预设本体来源目录(默认 ./preset)
```

从 **npm 装的副本**里同样能跑(`install.mjs` 与 `preset/` 都在 `files` 白名单里),
所以那条路线也不必手写受管段:

```powershell
node node_modules\dsh-plugin-dev-workflow\install.mjs --profile <profile 目录>
```

受管段用 `dev-workflow:managed:begin/end` 哨兵括起,`--uninstall` 整段删除,文件里其余内容
(**包括你手写的其它 patch 条目**)一个字节不动。跑第二次是幂等的(替换已有段)。

三条要记住的:

- ⚠️ **组合层是启动时读的** —— 写完要重启 DSH。
- ⚠️ 按 id 覆盖 `config` 是**整体替换**(`app-boot` 的 `applyEntryPatches` 逐键赋值,不深合并),
  所以受管段把基座的 `agent-presets.config` **逐字继承**过来再追加 `roots`。漏掉 schema 里必填的
  `default`,后果是整棵插件树加载失败(`invalid config: $.default missing required value`)。
- ⚠️ **桌面端会强制覆写那一行**:`dsh-plugin-desktop` 启动 profile 时会追加一层 launcher
  patch,把 id **恰为** `agent-presets` 的那一行 `config.roots` 换成它自己的两个根
  (shipped + `$DSH_HOME/.agent-presets`)。`config:` 是整体替换,所以**直接给基座那行写
  `roots` 永远不会生效** —— 名册里看不到「工作流模式」,而 `dsh --dump-config` 却显示它
  写对了(那条命令走 CLI,不带这层)。因此受管段把基座行 `disabled: true`,再用
  `id: agent-presets-profile` 插一行同包的自己的行:桌面端不认这个 id,`roots` 才留得住。
- ⚠️ 预设本体**随仓库与 npm 包一起走**(`preset/dev-workflow/`,已列入 `files`),
  profile 里没有时 `install.mjs` 会铺一份过去 —— 所以它不会再「删了就没了」。

### 8.1 方式一:`dsh plugin`(推荐 —— DSH 官方入口)

`dsh plugin <args>` 把参数**转发给 profile 目录里的 pnpm**,装完后**按已装状态对账层清单**:
依赖里声明了 `dsh.bundle` 的包会**自动加入 `dsh.profile.bundles`** —— 也就是**不用你手写激活行**。
profile 不存在时会先初始化(`dsh plugin --profile <新名> add <包>`)。

```powershell
dsh plugin --profile web add dsh-plugin-dev-workflow                  # 从 npm 装(需先发布,见 8.3)
dsh plugin --profile web add .\dsh-dev-workflow                        # 从本地克隆目录装(开发期)
dsh plugin --profile web add .\dsh-plugin-dev-workflow-<版本>.tgz      # 从 tgz 装(离线/内网)
dsh plugin --profile web remove dsh-plugin-dev-workflow               # 卸载
```

装完**重启 DSH** 即成。

### 8.2 方式二:npm(裸装 / 离线铺包)

适用于**不进 profile 依赖清单**的场景。

```powershell
npm pack                                    # ① 在源码目录产出 dsh-plugin-dev-workflow-<版本>.tgz
$dst = "$env:USERPROFILE\.dsh\profiles\web\node_modules\dsh-plugin-dev-workflow"
New-Item -ItemType Directory -Force -Path $dst | Out-Null
tar -xzf .\dsh-plugin-dev-workflow-<版本>.tgz -C $env:TEMP
Copy-Item "$env:TEMP\package\*" $dst -Recurse -Force   # ② 铺进 node_modules
# ③ 这条路不在 bundles 里,**不会自动激活** → 自己写一行 insert,见 8.4
```

> ⚠️ **别在 profile 目录里跑 `npm install`** —— 那是个 pnpm 工程
> (`pnpm-lock.yaml` + `pnpm-workspace.yaml`,且 `nodeLinker: hoisted`)。
> npm 会另建 `package-lock.json` 并可能与 pnpm 的 node_modules 结构打架。要用包管理器就走 8.1。

### 8.3 发布到 npm(一次性前置)

| 项 | 状态 |
|---|---|
| `private: true` | **已去掉** —— 留着会让 `npm publish` 直接拒 |
| 包名 | `dsh-plugin-dev-workflow`,实测在 npm 上**未被占用**(2026-09-13 查) |
| `files` 白名单 | `lib/`、`skills/`、`preset/`、`install.mjs`、`cordis.patch.yml`、`使用说明.md` |
| 测试套件 | `selftest.mjs` + `smoke.mjs` **刻意不进 npm 包**(省 335 KB);`prepublishOnly` 会在发布前先跑它们 |
| `license` | **MIT**（`package.json` 已声明；仓库根另有 `LICENSE` 全文） |
| 发布 | `npm publish --access public`;之后 `npm version patch && npm publish` |

### 8.4 随包的两件安装声明(这张表决定"dsh 认不认领它")

| 文件 / 字段 | 作用 |
|---|---|
| `cordis.patch.yml`(包根) | 本插件的**激活行**:`- insert: [- id: dev-workflow, name: dsh-plugin-dev-workflow]` |
| `package.json` 的 `dsh.bundle.patch` | 指向上面那份 —— **DSH 认领它的钥匙** |
| profile `cordis.patch.yml` 里的同 id `insert` 行 | **功能等价的手工替代**;与 bundle 路线**二选一** |

**为什么 `dsh.bundle.patch` 是钥匙**(读 DSH 实现得到的结论,不是推断):`dsh plugin` 装完后按**已装状态**
对账层清单 —— 声明了 `dsh.bundle` 的依赖**自动进 `dsh.profile.bundles`**;没声明的只会被装成**普通依赖**,
并打一行 `declares no dsh.bundle — installed as a plain dependency, not a profile layer`,**插件不会被加载**。
DSH 社区市场(`settings.yaml` 的 `dsh-community-market`)同样要求条目的 npm `latest` manifest 里
有合法的 `dsh.bundle.patch`,否则不允许安装。

> ⚠️ **同一个 id 只能挂一次。** 本机当前用的是**手工 insert 行**(见 8.5 的表)。若对同一个包再执行
> `dsh plugin add`,bundle 层会再加一条 → 同一 id 挂两次。要改成 `dsh plugin` 管:
> **先**删掉 profile `cordis.patch.yml` 里那段 insert,**再** `add`。

### 8.5 当前安装状态与文件位置

| 项 | 位置 |
|---|---|
| 插件(生效) | `~/.dsh/profiles/web/node_modules/dsh-plugin-dev-workflow/` |
| 随包技能 | 同上的 `skills/`(**24 份**):`api-architect`(`SKILL.md` + `references/` 5 份)+ 23 份角色联动技能(见 §3.5;含各自的 `references/`、`templates/`、`scripts/`) |
| 组合补丁行 | `~/.dsh/profiles/web/cordis.patch.yml` 的 `- insert: id=dev-workflow` |
| 插件私有状态 | `${DSH_HOME:-~/.dsh}/dev-workflow/state.json`(等待图/熔断窗口/角色绑定/台账/仲裁/计数/最近一次契约 lint/**开工记忆 `activeProjects`**) |
| 自定义档案 | `${DSH_HOME:-~/.dsh}/dev-workflow/profiles.json`(可选) |
| 人工入口预设 | **默认不安装**。要人工以角色身份开会话,按 §五「自定义档案」自行建五个角色预设,放进 `~/.dsh/.agent-presets/` |
| 项目侧文件 | `流程状态.md`、`协作台账.md`、各角色产出文档、`docs/api/`(契约);`docs/workflow/.active` **可选**(显式激活 / `stateName=` / `profile=` / `off`) |
| 源码工作区 | 本仓库根目录(克隆下来的那个目录) |
| 随包激活声明 | `cordis.patch.yml`(包根)+ `package.json` 的 `dsh.bundle.patch`(见 8.4) |
| 回归测试 | 四套,都不依赖 DSH:`node selftest.mjs`(**276**,功能本体纯逻辑)+ `node smoke.mjs`(**779**,功能本体端到端;装了人工入口预设时 +5 条同源校验)+ `node switch.selftest.mjs`(**10**,总开关:预设搬迁 + 挂载/卸下)+ `node switch.smoke.mjs`(**12**,开关卡片)。**从安装位置复跑** `smoke.mjs` 时加 `DSH_SMOKE_TMP=<可写目录>`。⚠️ 套件里**不许用递归 `fs.cpSync`**(目标在工作区之外时抛 EIO、甚至让 node fail-fast;用 `copyTree`) |
| 版本自证 | `relay action=status` 首屏会打印 `插件:dev-workflow vX.Y.Z(本进程已加载)`;若它与磁盘 `package.json` 不一致,status 与宿主日志都会告警 —— 这就是"部署了但没重启"的信号 |
| 私有状态的保护 | `state.json` **读不出来**时本进程一律**锁存不落盘**(绝不覆盖);**内容损坏**时先隔离成 `state.json.bad-<时间戳>` 再以空状态继续 |

### 8.6 装完怎么验

```powershell
dsh plugin --profile web list | Select-String dsh-plugin-dev-workflow      # ① 包在不在 profile 里
dsh --profile web --dump-config | Select-String -Context 2,2 dev-workflow   # ② 组合里有没有那一行
```

③ 重启后看 `relay action=status` 首屏的 `插件:dev-workflow vX.Y.Z(本进程已加载)`:版本要与磁盘 `package.json` 一致(不一致 = 部署了但没重启)。

### 8.7 改动生效规则

插件的工具逻辑是 host 静态行,**改完(或刚装完)要重启 DSH 才生效**;
`profiles.json` / `.active` / 项目文档是数据,**改了立刻生效**。
